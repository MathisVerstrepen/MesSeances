{
  "worktree": {
    "setup": [
      "cp \"$PASEO_SOURCE_CHECKOUT_PATH/deploy/.env\" deploy/.env",
      "cp \"$PASEO_SOURCE_CHECKOUT_PATH/AGENTS.md\" AGENTS.md",
      "cp -a -- \"$PASEO_SOURCE_CHECKOUT_PATH/.opencode/.\" ./.opencode",
      "cp -a -- \"$PASEO_SOURCE_CHECKOUT_PATH/tmp/.\" ./tmp",
      "cp -- \"$PASEO_SOURCE_CHECKOUT_PATH/.cbmignore\" ./.cbmignore",
      "cd api && go install ./...",
      "npm --prefix web ci"
    ]
  },
  "metadataGeneration": {
    "branchName": {
      "instructions": "Generate only the branch name, using the lowercase format <type>/<short-kebab-case-description>. Use one of these type prefixes: feat, fix, test, docs, refactor, chore, style, perf. Base feature branches on dev. Keep the description concise and task-specific; use no spaces or underscores, and avoid generic names."
    },
    "commitMessage": {
      "instructions": "Use the Conventional Commits format. Valid types: feat, fix, test, docs, refactor, chore, style, perf. Example: feat(invoice): add amount validation. The first line should not exceed 72 characters. The second line should be blank. Starting with the third line, write a more detailed description in explaining the reason for the change and any additional context. Use bullet points if there are several important points."
    },
    "pullRequest": {
      "instructions": "First determine the pull request base/head relationship. If a feature branch targets dev, read and follow .github/PULL_REQUEST_TEMPLATE/feature.md. If dev targets main, read and follow .github/PULL_REQUEST_TEMPLATE/release.md. Preserve the exact heading order from the selected file, fill it with factual content only, remove all Markdown comments and placeholders, do not mix templates, and do not invent sections. For a release pull request titled exactly Release X.Y.Z, generate a description that exactly matches tracked docs/changelogs/X.Y.Z.md, including LF line endings and one final newline. Use - None for every empty release section. Append a direct same-repository corresponding feature pull request link to each non-None release bullet when commit-associated repository evidence proves the relationship; never invent a link. In the feature template, report the exact tests or checks run and their outcomes; if none were run, state that fact."
    }
  }
}
